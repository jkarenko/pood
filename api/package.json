{
  "name": "pood-api",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/functions/*.js",
  "scripts": {
    "build": "esbuild src/functions/*.ts --bundle --platform=node --target=node22 --format=esm --outdir=dist/functions --external:@azure/functions",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0"
  },
  "devDependencies": {
    "@azure/data-tables": "^13.3.0",
    "@azure/storage-blob": "^12.26.0",
    "@types/node": "^25.5.0",
    "esbuild": "^0.27.4",
    "typescript": "~5.9.3"
  }
}
